# If you want load any configuration during teh aplication startup itself then we need this properties file 
spring.application.name=service-one
#this property is for which the spring cloud config server is running 
spring.cloud.config.uri=http://localhost:8083
management.endpoint.refresh.enabled=true
management.endpoint.info.enabled=true
management.endpoint.features.enabled=true
management.endpoint.health.enabled=true
management.endpoints.web.exposure.include=health,info,refresh
encrypt.key=myencryptionkey